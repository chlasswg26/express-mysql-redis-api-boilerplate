# express-mysql-redis-api-boilerplate

Quickstart for an Express.js api prototype backed by a MySQL database and Redis cache.

# Pre-requisites

* NodeJS
* Docker

# Installation

`npm install` or `yarn install`

# 